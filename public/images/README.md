# Public Images Directory
Store your public application images here (e.g. logos, product images, categories, banners).
Accessible in web browser via: `/images/...` or Laravel helper `asset('images/...')`.
